<h1>Editing Book</h1>
<%= form_with model: @book, url: "/books/#{@book.id}", local:true do |f| %>
<br>Title</br>
<%= f.text_field :title %>
<br>Body</br>
<%= f.text_area :body %>
<%= f.submit"Update Book" %>

<p><%= link_to "show", "/books/#{@book.id}" %>|<%= link_to "Back", books_path %></p>
<% end %>
